DATABASE_URL=postgresql+psycopg2://avook:avookpass@db:5432/avook
SECRET_KEY=canvia-aquesta-clau
ABS_HOST=localhost:13378      # domini públic on escolta Audiobookshelf
ABS_API_BASE_URL=             # (opcional) host intern quan el middleware corre dins Docker (p. ex. http://audiobookshelf)
ABS_USERNAME=                 # (opcional) usuari per obtenir un token de l'API
ABS_PASSWORD=                 # (opcional) contrasenya per al compte anterior
ABS_SHARE_FALLBACK_BASES=     # (opcional) URLs addicionals (separades per comes) per provar quan ABS no respon
URL_TTL_HOURS=4              # vigència del token en hores
